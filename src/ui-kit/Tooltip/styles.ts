import styled from 'styled-components';
import {Tooltip} from 'antd';

import {TextNote} from '../Typography';

export const StyledTooltip = styled(Tooltip)``;

export const StyledTextNote = styled(TextNote)``;
